# Pinned to the versions the model in models/best_model.joblib was trained and
# pickled with. Loosening these risks joblib.load failing on a version mismatch.
pandas==3.0.5
numpy==2.5.2
scikit-learn==1.9.0
xgboost==3.4.1
rdkit==2026.3.6
matplotlib==3.11.1
scipy==1.18.1
joblib==1.6.0
streamlit==1.63.0
